[github]
token = "github_pat_..."
repo  = "gustavomisk-dev/leads-sworks-data"

[auth]
# String aleatória de 32+ chars para assinar os cookies de sessão
secret = "troque-por-string-aleatoria-de-32-chars"

[auth.users.claudio]
email         = "claudio@zilicred.com.br"
password_hash = ""   # preencher com hash bcrypt (ver instrucoes abaixo)
display_name  = "Cláudio"

# ── Como gerar o password_hash ────────────────────────────────────────────────
# Execute em qualquer terminal Python:
#
#   import bcrypt
#   print(bcrypt.hashpw(b"sua-senha-aqui", bcrypt.gensalt(12)).decode())
#
# Cole o resultado em password_hash acima.
